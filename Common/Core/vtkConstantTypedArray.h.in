// SPDX-FileCopyrightText: Copyright (c) Ken Martin, Will Schroeder, Bill Lorensen
// SPDX-License-Identifier: BSD-3-Clause
/**
 * @class   vtkConstant@VTK_TYPE_NAME@Array
 * @brief   constant implicit array of @CONCRETE_TYPE@.
 *
 * vtkConstant@VTK_TYPE_NAME@Array is a constant implicit array of values of type @CONCRETE_TYPE@.
 * see vtkConstantArray, vtkConstantImplicitBackend
 *
 * This file is generated by vtkTypeArrays.cmake
 */

#ifndef vtkConstant@VTK_TYPE_NAME@Array_h
#define vtkConstant@VTK_TYPE_NAME@Array_h

#include "vtkConstantArray.h"    // Real Superclass
#include "vtkCommonCoreModule.h" // For export macro
#include "vtkDeprecation.h"      // For VTK_DEPRECATION_9_6_0
#include "vtkDataArray.h"

// Fake the superclass for non-Python wrappers.
// Python can handle the templated superclass; Java and others cannot.
#if !defined(__VTK_WRAP__) || defined(__VTK_WRAP_PYTHON__)
#define vtkDataArray vtkConstantArray<@CONCRETE_TYPE@>
#endif
VTK_ABI_NAMESPACE_BEGIN
class @VTK_DEPRECATION@ VTKCOMMONCORE_EXPORT vtkConstant@VTK_TYPE_NAME@Array : public vtkDataArray
{
public:
  vtkImplicitArrayTypeMacro(vtkConstant@VTK_TYPE_NAME@Array, vtkDataArray);
#if !defined(__VTK_WRAP__) || defined(__VTK_WRAP_PYTHON__)
#undef vtkDataArray
#endif

  // This macro expands to the set of method declarations that
  // make up the interface of vtkConstantArray, which is ignored
  // by the wrappers.
#if (defined(__VTK_WRAP__) || defined(__WRAP_GCCXML__)) && !defined(__VTK_WRAP_PYTHON__)
  vtkCreateConstantWrappedArrayInterface(@CONCRETE_TYPE@);
#endif

  static vtkConstant@VTK_TYPE_NAME@Array* New();
  static vtkConstant@VTK_TYPE_NAME@Array* ExtendedNew();
  void PrintSelf(ostream& os, vtkIndent indent) override;

  /**
   * A faster alternative to SafeDownCast for downcasting vtkAbstractArrays.
   */
  static vtkConstant@VTK_TYPE_NAME@Array* FastDownCast(vtkAbstractArray* source)
  {
    return static_cast<vtkConstant@VTK_TYPE_NAME@Array*>(Superclass::FastDownCast(source));
  }

protected:
  vtkConstant@VTK_TYPE_NAME@Array();
  ~vtkConstant@VTK_TYPE_NAME@Array() override;

private:
  typedef vtkConstantArray<@CONCRETE_TYPE@> RealSuperclass;

  vtkConstant@VTK_TYPE_NAME@Array(const vtkConstant@VTK_TYPE_NAME@Array&) = delete;
  void operator=(const vtkConstant@VTK_TYPE_NAME@Array&) = delete;
};

// Define vtkArrayDownCast implementation:
vtkArrayDownCast_FastCastMacro(vtkConstant@VTK_TYPE_NAME@Array);

VTK_ABI_NAMESPACE_END
#endif
// VTK-HeaderTest-Exclude: TYPEMACRO
