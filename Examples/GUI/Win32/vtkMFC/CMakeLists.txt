PROJECT(vtkMFC)

FIND_PACKAGE(VTK)
IF(NOT VTK_DIR)
  MESSAGE(FATAL_ERROR "Please set VTK_DIR.")
ENDIF()
INCLUDE(${VTK_USE_FILE})

ADD_SUBDIRECTORY(vtkMDI)
ADD_SUBDIRECTORY(vtkSDI)
ADD_SUBDIRECTORY(vtkDLG)
