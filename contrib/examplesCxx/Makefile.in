#
# examples/cc//Makefile.in  --
#
# Makefile for Visualization Toolkit C++ examples.
# 
#------------------------------------------------------------------------------
#
SHELL = /bin/sh
.SUFFIXES: .cxx

srcdir        = @srcdir@
VPATH         = @srcdir@
VTK_LIB_FILE  = @VTK_LIB_FILE@

#------------------------------------------------------------------------------
# Autoconfig defines that can be overridden in user.make

@MAKEINCLUDE@ @MAKEQUOTE@../../system.make@MAKEQUOTE@

#------------------------------------------------------------------------------
# Include user-editable defines.

@MAKEINCLUDE@ @MAKEQUOTE@../../user.make@MAKEQUOTE@

#-----------------------------------------------------------------------------

EXAMPLES = TestMPI TestController TestPorts ParallelIso 
  
CXX_FLAGS = ${CPPFLAGS} ${USER_CXXFLAGS} ${CXXFLAGS} -I${srcdir} \
	${KIT_FLAGS} -I. ${GRAPHICS_API_FLAGS} \
	 -I${srcdir}/../../common -I${srcdir}/.. -I${srcdir}/../../patented \
	-I${srcdir}/../../imaging -I${srcdir}/../../graphics

VTKLIB = ${VTK_LIB_FILE}

LDLIBS = ${VTK_LIB_FILE} ${XLIBS} ${GRAPHICS_API_LIBS} ${DL_LIBS} ${THREAD_LIBS} -L/home/lawcc/mpich/build/LINUX/ch_p4/lib -lpmpich -lmpich -lXext -lXt -lSM -lICE -lX11  -lm

.cxx.o:
	${CXX} ${CXX_FLAGS} -c $< -o $@

#------------------------------------------------------------------------------
#

all: ${EXAMPLES}

TestMPI: TestMPI.o ${VTKLIB}
	${CXX} -o TestMPI ${CXX_FLAGS} TestMPI.o ${XLDFLAGS} ${LDLIBS} \
	${XLIBS} -lXext -lXt ${X_PRE_LIBS} -lX11 ${X_EXTRA_LIBS} -lm

TestController: TestController.o ${VTKLIB}
	${CXX} -o TestController ${CXX_FLAGS} TestController.o ${XLDFLAGS} ${LDLIBS} \
	${XLIBS} -lXext -lXt ${X_PRE_LIBS} -lX11 ${X_EXTRA_LIBS} -lm

TestPorts: TestPorts.o ${VTKLIB}
	${CXX} -o TestPorts ${CXX_FLAGS} TestPorts.o ${XLDFLAGS} ${LDLIBS} \
	${XLIBS} -lXext -lXt ${X_PRE_LIBS} -lX11 ${X_EXTRA_LIBS} -lm

ParallelIso: ParallelIso.o ${VTKLIB}
	${CXX} -o ParallelIso ${CXX_FLAGS} ParallelIso.o ${XLDFLAGS} ${LDLIBS} \
	${XLIBS} -lXext -lXt ${X_PRE_LIBS} -lX11 ${X_EXTRA_LIBS} -lm

#------------------------------------------------------------------------------

clean:
	-rm -f ${EXAMPLES} *.o 

