## Internalize `DefaultModNames` members

The protected `vtkArrayRenderer::DefaultModNames` is no longer available.

The protected `vtkOpenGLLowMemoryPolyDataMapper::DefaultModNames` is no longer
available.
