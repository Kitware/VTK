## vtkTestingInteractor statics

The following public static members of `vtkTestingInteractor` are now
deprecated in favor of setters (`Set*`) and getters (`Get*`):

- `TestReturnStatus`
- `ErrorThreshold`
- `ValidBaseline`
- `TempDirectory`
- `DataDirectory`
