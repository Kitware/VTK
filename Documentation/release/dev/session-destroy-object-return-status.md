# Session Destroy Object Return Status

The `vtkStandaloneSession::Destroy` method now returns a `bool` status indicating the success (`true`) or failure (`false`) of the object destruction operation.
