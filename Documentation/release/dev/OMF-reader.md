## Add vtkOMFReader

Adds reader for [Open Mining Format](https://omf.readthedocs.io/en/stable/index.html) files.
The reader outputs a vtkPartitionedDataSetCollection, where each vtkPartitionedDataSet is one
OMF element (point set, line set, surface, or volume).
