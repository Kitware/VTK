# Change the type of debug message when setting the use of log scale with an invalid table range

In a `vtkLookUpTable`, if a Log Scale is set with an invalid table range, the log message has changed to `Warning`.
