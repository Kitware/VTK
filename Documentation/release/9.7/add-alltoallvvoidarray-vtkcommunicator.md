# Add new AllToAllVVoidArray method in vtkCommunicator

The `AllToAllVVoidArray` from MPI is now available in `vtkCommunicator`. This function sends data from all processes to all processes. Each process can send a different amount of data.
