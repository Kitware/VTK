## VTK SDK as wheels are now available on the wheel-sdks repository

The VTK wheel SDK used to be distributed as tar.xz archives.

It is now packaged directly as python wheels (.whl) that can be consummed as-is by pip, the python package manager.
