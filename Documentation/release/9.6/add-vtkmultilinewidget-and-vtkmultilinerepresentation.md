## Add a new widget to handle a configurable number of line

`VTK` now has `vtkMultiLineWidget` class that handles a configurable number of `vtkLineWidget2`. The representation of this class is a `vtkMultiLineRepresentation` which displays the corresponding number of `vtkLineRepresentation`.
