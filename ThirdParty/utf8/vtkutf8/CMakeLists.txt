if (NOT VTK_NO_INSTALL_DEVELOPMENT)
  install(
    FILES       "${CMAKE_CURRENT_SOURCE_DIR}/utf8.h"
    DESTINATION "${VTK_INSTALL_INCLUDE_DIR}/vtkutf8")
  install(
    DIRECTORY   "${CMAKE_CURRENT_SOURCE_DIR}/utf8"
    DESTINATION "${VTK_INSTALL_INCLUDE_DIR}/vtkutf8")
endif ()
