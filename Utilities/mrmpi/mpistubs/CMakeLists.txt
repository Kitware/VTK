SET (mpi_SOURCES mpi.cpp)

ADD_LIBRARY (mpi ${mpi_SOURCES})
TARGET_LINK_LIBRARIES (mpi vtksys)
IF(VTK_LIBRARY_PROPERTIES)
  SET_TARGET_PROPERTIES(mpi PROPERTIES ${VTK_LIBRARY_PROPERTIES})
ENDIF(VTK_LIBRARY_PROPERTIES)
