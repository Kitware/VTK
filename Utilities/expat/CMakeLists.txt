PROJECT(VTKEXPAT)

SOURCE_FILES(common_SRCS 
  xmlparse.c 
  xmltok.c 
  xmlrole.c
)

INCLUDE_DIRECTORIES(${VTKEXPAT_SOURCE_DIR})
INCLUDE_DIRECTORIES(${VTKEXPAT_BINARY_DIR})

IF (NOT VTK_LIBRARY_PATH)
  LINK_DIRECTORIES(${VTKEXPAT_BINARY_DIR})
ENDIF (NOT VTK_LIBRARY_PATH)

IF(WIN32)
  IF(BUILD_SHARED_LIBS)
    ADD_DEFINITIONS(-DVTK_EXPAT_SHARED)
  ELSE(BUILD_SHARED_LIBS)
    ADD_DEFINITIONS(-DVTK_EXPAT_STATIC)
  ENDIF(BUILD_SHARED_LIBS)
  IF (NOT BORLAND)
      IF (NOT UNIX)
	 SOURCE_FILES(common_SRCS vtkexpat.def)
      ENDIF(NOT UNIX)
   ENDIF(NOT BORLAND)
ENDIF(WIN32)

CONFIGURE_FILE(${VTKEXPAT_SOURCE_DIR}/expatConfig.h.in
               ${VTKEXPAT_BINARY_DIR}/expatConfig.h)
CONFIGURE_FILE(${VTKEXPAT_SOURCE_DIR}/.NoDartCoverage 
               ${VTKEXPAT_BINARY_DIR}/.NoDartCoverage)

ADD_LIBRARY(vtkexpat common_SRCS)

INSTALL_TARGETS(/lib/vtk vtkexpat)
